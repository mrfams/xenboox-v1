# Pipeline 2: Bank Connection → Transaction Import → Categorization

## Sub-Part F — UI Layer — Fix Verification

**Audit:** `findings/pipeline-2-banking/subpart-f-ui-layer/audit-findings.md`

---

### Fixes Applied

| #      | Finding                                                                                                                                                        | Fix                                                                                                                                                                                                                                                                | Evidence                                             |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- |
| **F1** | 🔴 `format` called but never defined — transaction amounts could not render                                                                                    | Wired `useFormatCurrency`; amounts + balances now format with the **transaction's own currency symbol** (`format(tx.amount, tx.currency)`).                                                                                                                        | `transaction-row.tsx`                                |
| **F2** | 🔴 Rules UI match types (`contains`/`starts_with`/`exact`/`regex`) don't exist in the API enum — every create/update failed zod validation; Rules tab was dead | `MATCH_TYPES` now mirror the API exactly: `description_contains`, `description_equals`, `reference_contains`, `amount_equals`, `amount_above`, `amount_below` (with per-type hints); `Rule.matchType` typed as the union; form defaults to `description_contains`. | `bank-rules-manager.tsx`                             |
| **F3** | 🔴 Export CSV read nonexistent `transactionDate`/`status` fields                                                                                               | Export uses `t.date` and derives status from `t.isReconciled`.                                                                                                                                                                                                     | `banking-view.tsx`                                   |
| **H1** | 🟠 Direction display lied after P2-B (`amount > 0` is always true → every row a green deposit)                                                                 | Direction comes from `tx.type === "deposit"`; deposits `+` green down-arrow, withdrawals `−` default-color up-arrow.                                                                                                                                               | `transaction-row.tsx`                                |
| **H2** | 🟠 Confidence multiplied as a number while arriving as a string → NaN widths/titles                                                                            | Defensive `Number(tx.categorizationConfidence)`; all display math via `confidencePct`; width clamped to 100%.                                                                                                                                                      | `transaction-row.tsx`                                |
| **H3** | 🟠 "AI automatically creates a rule after 3 overrides" was advertised but unimplemented                                                                        | `updateTransactionCategory` now promotes a pattern to a real `description_contains` rule when `timesSeen >= 3` (deduped against existing rules, audit-logged as `banking.autoCreateRule`, skip descriptions < 8 chars). The UI claim is now true.                  | `server/routers/banking.ts`                          |
| **H4** | 🟠 `entityId: string \| null` → `string` at 3 sites; `BankConnectionCard` Connection type mismatched the router (Date vs ISO string, loose unions)             | Tab components accept `string \| null` (queries already gate on `!!entityId`); Connection type mirrors `listConnections` (ISO-string timestamps, provider/status enums).                                                                                           | `banking-view.tsx`, `bank-connection-card.tsx`       |
| **M1** | 🟡 `CategoryPicker currentCategory` rejected optional category                                                                                                 | Call site passes `tx.category ?? null`.                                                                                                                                                                                                                            | `transaction-row.tsx`                                |
| **M2** | 🟡 Native `confirm()` for destructive actions                                                                                                                  | Replaced with shadcn `AlertDialog` confirmations in the rules delete + connection disconnect flows (matches `conflict-resolution.tsx` pattern).                                                                                                                    | `bank-rules-manager.tsx`, `bank-connection-card.tsx` |

---

### Verification

| Check                                                                                                                                     | Result                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web` tsc — banking components (`banking-view`, `transaction-row`, `bank-connection-*`, `bank-rules-manager`, `create-bank-account`) | ✅ **0 errors** (was 12: 6 banking-view, 3 transaction-row, 3 rules-manager)                                                                                                                                                     |
| Web banking suites (4 files: banking*, *bank*, reconciliation*)                                                                           | ✅ 38 tests pass (31 existing + 7 new `banking-ui-integrity`)                                                                                                                                                                    |
| `banking-ui-integrity.test.ts` guards                                                                                                     | ✅ 7 static assertions: currency hook wired, direction from `type` (not sign), defensive confidence, match types = API enum (legacy values banned), export fields real, AlertDialog (no `confirm(`), auto-rule promotion present |
| Full web suite baseline                                                                                                                   | 58 failures unchanged — all pre-existing (agents `@/` alias + env-dependent), zero banking-file failures                                                                                                                         |

**Behavior now:**

- Transaction list renders real currency amounts with correct deposit/withdrawal direction and working confidence bars.
- Rules tab can actually create/update/delete rules (match types match the API).
- After 3 manual overrides of the same vendor, the AI creates a rule automatically — the advertised behavior is real.
- Export CSV works; disconnect/delete confirmations match the product's dialog pattern.
